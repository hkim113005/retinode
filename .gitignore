# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
.installed.cfg
*.egg

# Virtual environments
.venv/
venv/
env/
ENV/
# NOTE: .python-version is intentionally NOT ignored — uv uses it to pin the
# project interpreter (3.12), and committing it keeps the env reproducible.

# Testing / coverage
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/

# Jupyter
.ipynb_checkpoints/

# Retinode project data (generated; not source)
# Project stores hold caches, results, and provenance — regenerable artifacts.
*.retinode/cache/
*.retinode/results/
project.retinode/cache/
project.retinode/results/

# Simulation / solver artifacts
*.hdf5
*.h5
*.parquet
# gmsh meshes
*.msh
*.xdmf
# ADIOS/DOLFINx output
*.bp/
# NEURON compiled mechanisms (nrnivmodl output: x86_64, Apple Silicon, ARM Linux)
x86_64/
arm64/
aarch64/
*.o
# NEURON-generated C from .mod files
*.c

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~

# Secrets / local config
.env
.env.local
*.local

# Claude Code local settings (per-user, not shared)
.claude/settings.local.json
